model:
  - id: ${deviceId} # eg 'hmi1'
    name: Rockwell-HMI
    uuid: ${uuid} # eg 'e05363a'
    sampleInterval: 10

    description:
      manufacturer: Rockwell
      model: Rockwell HMI
      serialNumber: ${serialNumber} # eg '001'
      text: Rockwell HMI

    dataItems:
      dataItem:
        - connection
        - asset_changed
        - asset_removed
        - operator
