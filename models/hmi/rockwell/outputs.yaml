# id is formed from deviceId + key, eg 'pa1-operator'
# value is javascript
# <foo> is a cache lookup operation - shorthand for cache.get('${deviceId}-foo')

outputs:
  - key: operator
    category: EVENT
    type: USER
    subType: OPERATOR
    value: <operator>
