# id is formed from deviceId + key, eg 'ccs-pa-001-operator'
# value is javascript
# <foo> is a cache lookup operation - shorthand for cache.get('${deviceId}-foo')

outputs:
  - key: operator
    category: EVENT
    type: USER
    subType: OPERATOR
    value: <operator>
