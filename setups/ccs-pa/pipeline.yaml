# docker-compose file for ladder99 pipeline.
# specifies overrides for services listed in setups/pipeline.yaml.

#. this will be generated from devices.yaml, but edit manually for now.

version: '3.8'

# # uncomment these lines and run pipeline to see xml output
# services:
#   agent:
#     volumes:
#       - ./$SETUP/volumes/agent-orig:/data/agent # has ccs-pa agent.cfg, devices.xml

networks:
  ladder99:
    name: ladder99
