  # timescaledb:
  #   container_name: timescaledb
  #   image: timescale/timescaledb:2.1.1-pg12
  #   environment:
  #     POSTGRES_PASSWORD: 'gralgrut'
  #   volumes:
  #     - db-data:/var/lib/postgresql/data
  #   ports:
  #     - '5433:5432' # 5432 was taken by... ?
  #   networks:
  #     - ladder99
  #   logging:
  #     options:
  #       max-size: '1m'

  # telegraf:
  #   image: telegraf
  #   links:
  #     - influxdb
  #   volumes:
  #     # Mount for telegraf config
  #     - ../volumes/telegraf/mytelegraf.conf:/etc/telegraf/telegraf.conf
  #   depends_on:
  #     - influxdb_cli

  # grafana:
  #   container_name: grafana
  #   image: grafana/grafana:latest
  #   ports:
  #     - '3001:3000' # 3000 taken by my tallieo
  #   volumes:
  #     # - ../volumes/grafana/config/grafana.ini:/etc/grafana/grafana.ini
  #     - ../volumes/grafana:/etc/grafana
  #     - grafana_data:/var/lib/grafana
  #     # - grafana_conf:/etc/grafana
  #   depends_on:
  #     # timescaledb:
  #     #   condition: service_started
  #     influxdb:
  #       condition: service_started
  #   networks:
  #     - ladder99
  #   logging:
  #     options:
  #       max-size: '1m'



  # influxdb:
  #   container_name: influxdb
  #   image: influxdb:2.0-alpine
  #   volumes:
  #     - influxdbv2:/.influxdbv2
  #   ports:
  #     - '8086:8086'
  #   networks:
  #     - ladder99
  #   logging:
  #     options:
  #       max-size: '1m'

  # # Use the influx cli to set up an influxdb instance.
  # influxdb_cli:
  #   container_name: influxdb_cli
  #   image: influxdb:2.0-alpine
  #   # The influx setup command walks through the initial InfluxDB OSS setup
  #   # process, creating a default user, organization, and bucket.
  #   # Use these same configurations parameters in your telegraf configuration,
  #   # mytelegraf.conf.
  #   entrypoint: influx setup --bucket ladder99 -t influxtoken -o ladder99 --username=bburns --password=whatisthis --host=http://influxdb:8086 -f
  #   # Wait for the influxd service in the influxdb container has fully bootstrapped
  #   # before trying to setup an influxdb instance with the influxdb_cli service.
  #   restart: on-failure:10
  #   networks:
  #     - ladder99
  #   depends_on:
  #     - influxdb

