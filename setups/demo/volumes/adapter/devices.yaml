# this defines the devices.xml structure, along with the model yamls.

devices:
  - id: ccs-pa-001
    model: ccs-pa
    properties:
      uuid: e05363af-95d1-4354-b749-8fbb09d3499e
      serialNumber: '001'
    sources:
      - inputs: ccs-pa # not really needed as empty
        type: mqtt-ccs # type of plugin - manages protocol and payload
        active: true # should this data source be initialized on adapter startup?
        # put url etc in a config dict?
        url: mqtt://broker-ccs-pa-001:1883
        user:
        password:
        tls: false

      - inputs: rockwell-hmi
        type: opc
        active: true
        url: opc.tcp://simulator-opc:4334/UA/LittleServer
        poll: 5

      - inputs: lapond-vfd
        type: opc
        active: true
        url: opc.tcp://simulator-opc:4334/UA/LittleServer
        poll: 5

      - inputs: beckhoff-mc
        type: opc
        active: true
        url: opc.tcp://simulator-opc:4334/UA/LittleServer
        poll: 5

      - inputs: cognex-camera
        type: text
        active: true
        url:
        poll: 5

      - inputs: asc-econoclave
        type: cpc
        active: true
        configuration:
          primary:
            ip: 192.168.2.2
            port: 9999
            poll: 5
          secondary:
            ip: 192.168.2.3
            port: 9999
            poll: 5

    destinations:
      - type: shdr
        host: adapter
        port: 7878
