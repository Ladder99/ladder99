# minimally need version number
version: '3.8'

services:
  adapter:
    profiles:
      - dashboard
  agent:
    profiles:
      - dashboard
  relay:
    profiles:
      - dashboard
  postgres:
    profiles:
      - dashboard
