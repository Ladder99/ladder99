apiVersion: 1

providers:
  - name: Ladder99
    folder: home
    type: file
    allowUiUpdates: true
    options:
      # <string, required> path to dashboard files on disk.
      # Required when using the 'file' type
      # path: /etc/grafana/dashboards
      path: /etc/dashboards
      foldersFromFilesStructure: true
