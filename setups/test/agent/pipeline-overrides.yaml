# docker-compose overrides for development pipeline

version: '3.8'

services:
  # we're using this to develop the agent html output
  agent:
    volumes:
      - ./$SETUP/volumes/agent:/data/agent # has agent.cfg, agent.xml
      - ../pipeline/agent/styles-0.1.0:/data/styles
