# docker-compose overrides

# note: don't need agent for this setup, since just polling agent endpoints

version: '3.8'

services:
  relay:
    environment:
      - AGENT_ENDPOINTS=https://smstestbed.nist.gov/vds/
