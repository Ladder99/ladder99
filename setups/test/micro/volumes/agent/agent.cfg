# MTConnect Agent configuration

Adapters {
   # adapter name here can be anything
   adapter {
      # this device value must match device name in agent.xml
      Device = micro1
      # note: don't include http in host
      Host = adapter
      Port = 7878
   } 
}

Devices = /data/agent/agent.xml

logger_config {
   # logging_level = warn
   logging_level = debug
}

# comment these blocks out to see xml output
# Files {
#    styles {
#       Path = /data/styles
#       Location = /styles
#    }
#    Favicon {
#       Path = /data/styles/favicon.ico
#       Location = /favicon.ico
#    }
# }
# DevicesStyle { Location = /styles/styles.xsl }
# StreamsStyle { Location = /styles/styles.xsl }

Files {
   styles {
      Path = /etc/mtconnect/styles
      Location = /styles
   }
   Favicon {
      Path = /etc/mtconnect/styles/favicon.ico
      Location = /favicon.ico
   }
}
DevicesStyle { Location = /styles/Devices.xsl }
StreamsStyle { Location = /styles/Streams.xsl }
