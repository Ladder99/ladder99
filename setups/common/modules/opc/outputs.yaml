# define outputs from cache to agent

#. most outputs could be pass-through, and wouldn't need to be specified here
# eg key=avail, value: <avail>

outputs:
  - key: avail
  - key: user1
  - key: user2

  - key: cond
    category: CONDITION
    value: "<user1>==='HELLO' && <user2> > 10 ? 'ERROR' : 'NORMAL'"
