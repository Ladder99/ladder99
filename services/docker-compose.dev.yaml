# docker compose yaml version - must be a string
# see https://docs.docker.com/compose/compose-file/compose-file-v3
# note: a '3' here means '3.0'
version: '3.8'

services:
  adapter:
    image: ladder99/adapter:dev

  meter:
    image: ladder99/meter:dev

  relay:
    image: ladder99/relay:dev

  simulator:
    image: ladder99/simulator:dev
